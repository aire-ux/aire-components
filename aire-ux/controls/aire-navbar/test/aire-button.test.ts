import { html, fixture, expect } from '@open-wc/testing';

import { AireNavbar } from '../src/AireNavbar.js';
// import '../aire-navbar.js';

describe('AireNavbar', () => {
  // it('has a default title "Hey there" and counter 5', async () => {
  //   const el = await fixture<AireNavbar>(html`<aire-navbar></aire-navbar>`);
  //
  //   expect(el.title).to.equal('Hey there');
  //   expect(el.counter).to.equal(5);
  // });
  //
  // it('increases the counter on navbar click', async () => {
  //   const el = await fixture<AireNavbar>(html`<aire-navbar></aire-navbar>`);
  //   el.shadowRoot!.querySelector('navbar')!.click();
  //
  //   expect(el.counter).to.equal(6);
  // });
  //
  // it('can override the title via attribute', async () => {
  //   const el = await fixture<AireNavbar>(html`<aire-navbar title="attribute title"></aire-navbar>`);
  //
  //   expect(el.title).to.equal('attribute title');
  // });
  //
  // it('passes the a11y audit', async () => {
  //   const el = await fixture<AireNavbar>(html`<aire-navbar></aire-navbar>`);
  //
  //   await expect(el).shadowDom.to.be.accessible();
  // });
});
